void pdlaprnt2(int* M, int* N, double* A, int* ia, int* ja, int* descA, int* irprnt, int* icprnt, char* matrixname, FILE* outfile, double* WORK, int len_m);
void pslaprnt2(int* M, int* N, float* A, int* ia, int* ja, int* descA, int* irprnt, int* icprnt, char* matrixname, FILE* outfile, float* WORK, int len_m);
